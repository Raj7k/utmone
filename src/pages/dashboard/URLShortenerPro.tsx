/**
 * URLShortenerPro Page - Authenticated Users
 * Advanced URL shortening with campaign management
 */

import { URLShortenerPro as URLShortenerProComponent } from '@/components/url-shortener/URLShortenerPro';

export default function URLShortenerPro() {
  return <URLShortenerProComponent />;
}
